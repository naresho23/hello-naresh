name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  name:  # id of input
    description: 'Who to greet'
    required: true
    default: 'Hello Naresh'
outputs:
  greeting: 
    description: 'The time we greeted you'
runs:
  using: 'node20'
  main: 'index.js'
